<%= form_for(@coupon_type) do |f| %>
  <% if @coupon_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@coupon_type.errors.count, "error") %> prohibited this coupon_type from being saved:</h2>

      <ul>
      <% @coupon_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <%= f.label :flag %><br />
    <%= f.check_box :flag %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
