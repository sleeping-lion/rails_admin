<%= form_for(@ad_comment) do |f| %>
  <% if @ad_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ad_comment.errors.count, "error") %> prohibited this ad_comment from being saved:</h2>

      <ul>
      <% @ad_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ad %><br />
    <%= f.text_field :ad %>
  </div>
  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
