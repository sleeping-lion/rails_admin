<%= form_for [:admin,@game_amount] do |f| %>
  <% if @game_amount.errors.any? %>
    <div id="error_explanation" class="alert alert-error">
    		<a href="#" class="close" data-dismiss="alert">&times;</a>
      <h2><%= pluralize(@game_amount.errors.count, "error") %> prohibited this sponsor from being saved:</h2>
      <ul>
      <% @game_amount.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :amount %>
    <%= f.number_field :amount,:class=>"form-control",:required=>"reqiured" %> <%=t(:label_point)%>
  </div>
  <div class="form-group">
    <%= f.label :flag %>
    <%= f.check_box :flag %>
  </div>	  
  <div class="form-group actions">
    <%= f.submit :class=>"btn btn-primary" %>
  </div>
<% end %>
