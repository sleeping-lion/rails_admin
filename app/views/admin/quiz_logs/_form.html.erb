<%= form_for [:admin,@quiz_log] do |f| %>
<% if @quiz_log.errors.any? %>
<div id="error_explanation" class="alert alert-danger">
	<a href="#" class="close" data-dismiss="alert">&times;</a>
	<h2><%= pluralize(@quiz_log.errors.count, "error") %> prohibited this point from being saved:</h2>
	<ul>
		<% @quiz_log.errors.full_messages.each do |msg| %>
		<li>
			<%= msg %>
		</li>
		<% end %>
	</ul>
</div>
<% end %>
<div class="control-group">
	<%= f.label :quizzes_id %>
	<%= f.text_field :quizzes_id,:required=>"required" %>
</div>
<div class="control-group">
	<%= f.label :quiz_answers_id %>
	<%= f.text_field :quiz_answers_id,:required=>"required" %>
</div>
<div class="control-group">
	<%= f.label :users_id %>
	<%= f.text_field :users_id,:required=>"required" %>
</div>
<div class="control-group actions">
	<%= f.submit :class=>"btn btn-primary" %>
</div>
<% end %>
